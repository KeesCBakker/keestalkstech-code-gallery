# Workflow

Building a workflow system with configurable steps and dependency injection.

This project explores how to structure workflows using .NET with support for
configurable steps and dependency injection.

## Features

- Support for .NET 10
- Workflow step abstraction
- Dependency injection integration
